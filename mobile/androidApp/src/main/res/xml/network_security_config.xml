<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: pin api.tamixa.in certificates -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.tamixa.in</domain>
        <domain includeSubdomains="true">tamixa.in</domain>
        <pin-set expiration="2026-12-31">
            <!-- Primary certificate SHA-256 pin (replace with actual cert fingerprint) -->
            <!-- Get fingerprint: openssl s_client -connect api.tamixa.in:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64 -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <!-- Backup certificate pin (use a different CA or backup cert) -->
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>

    <!-- Development: allow localhost and emulator traffic -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- Default: require HTTPS for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

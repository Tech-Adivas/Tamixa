/**
 * Canonical parent-app paths. Keep aligned with mobile
 * `Screen.LifeReadiness` (`life-readiness`) and library hub query values in `libraryHub.ts`.
 */
export const ROUTES = {
  lifeReadiness: "/life-readiness",
} as const;

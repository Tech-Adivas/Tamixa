# Backend API base URL (used when not using Vite proxy)
# Local dev: Vite proxy uses localhost:8080 - leave unset or use VITE_API_URL=http://localhost:8080
# Production build: VITE_API_URL=https://api.tamixa.com
# VITE_API_URL=http://localhost:8080

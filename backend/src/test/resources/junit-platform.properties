# Integration tests share one Postgres container; avoid parallel classes fighting one Spring context / JDBC URL.
junit.jupiter.execution.parallel.enabled=false
junit.jupiter.execution.parallel.mode.default=same_thread
